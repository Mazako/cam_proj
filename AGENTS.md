# Notatki dla agentów

- Pisz po polsku.
- Unikaj komentarzy w kodzie.
- Preferuj `rg` i `fd` zamiast `grep` i `find`.
- Duże moduły Rust dziel na katalog modułu; struktury domenowe trzymaj w osobnych plikach, a testy w osobnych plikach testowych, najlepiej w katalogu `tests/`, gdy korzystają wyłącznie z publicznego API.
- Projekt Camwatch i jego workspace Cargo znajdują się w repozytoryjnym root.
- Dokument przewodni aplikacji Rust jest w `docs/README.md`.
- Backlog oparty na specyfikacji jest w `docs/tasks.md`.
